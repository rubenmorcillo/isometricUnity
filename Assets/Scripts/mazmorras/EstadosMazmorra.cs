using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EstadosMazmorra : MonoBehaviour
{
    public bool combate;
    public bool explorar;
    void Start()
    {
        combate = false;
        explorar = false;
    }

    // Update is called once per frame
    void Update()
    {
        if (combate)
        {

        }else if (explorar)
        {

        }
    }
}
